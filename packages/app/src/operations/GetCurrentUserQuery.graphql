query GetCurrentUser {
  user {
    id
    username
    firstname
    surname
    thumb

    connections {
      id
      user {
        id
        firstname
        surname
        username
      }
      status
    }
  }
}
