query GetCurrentUser {
  user {
    id
    username
    firstname
    thumb

    connections {
      id
      user {
        id
        firstname
        surname
        username
      }
      status
    }
  }
}
