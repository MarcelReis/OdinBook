query UserPage($username: String!) {
  user(username: $username) {
    id
    name
    thumb
    username

    connectionStatus @client
    connections {
      id
      user {
        id
        firstname
        surname
        username
      }
      status
    }
  }
}
