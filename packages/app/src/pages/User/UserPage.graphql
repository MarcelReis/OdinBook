query UserPage($username: String!) {
  user(username: $username) {
    id
    name
    thumb

    connections {
      id
      user {
        id
        firstname
        surname
        username
      }
      status
    }
  }
}
