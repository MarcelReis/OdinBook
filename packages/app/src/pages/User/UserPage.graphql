query UserPage($username: String!) {
  user(username: $username) {
    id
    name @client
    firstname
    surname
    thumb
    username

    connectionStatus @client
    connections {
      id
      user {
        id
        firstname
        thumb
        surname
        username
      }
      status
    }
  }
}
