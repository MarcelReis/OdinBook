mutation CreatePost($content: String!) {
  createPost(content: $content) {
    posts {
      id
      content
      createdAt
      user {
        id
        name @client
        firstname
        surname
        username
      }
    }
  }
}
