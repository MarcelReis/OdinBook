mutation FinishRegistration($input: CreateUserInput!) {
  createUser(input: $input) {
    id
    username
    firstname
    thumb

    connections {
      id
      user {
        id
        firstname
        surname
        username
      }
      status
    }
  }
}
