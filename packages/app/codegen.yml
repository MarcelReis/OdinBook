overwrite: true
schema:
  - "../functions/lib/graphql/schema.graphql"
  - "./client-schema.graphql"
documents: "src/**/*.graphql"
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
