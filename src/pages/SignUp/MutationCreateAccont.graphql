mutation CreateAccount(
  $firstName: String!
  $surname: String!
  $email: String
  $password: String!
  $deityID: String!
) {
  createAccount(
    firstName: $firstName
    surname: $surname
    email: $email
    password: $password
    deityID: $deityID
  ) {
    id
    username
  }
}
