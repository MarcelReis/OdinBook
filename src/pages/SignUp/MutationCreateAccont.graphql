mutation CreateAccount(
  $firstName: String!
  $surname: String!
  $email: String
  $password: String!
  $deityURI: String!
) {
  createAccount(
    firstName: $firstName
    surname: $surname
    email: $email
    password: $password
    deityURI: $deityURI
  ) {
    id
    username
  }
}
