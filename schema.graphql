type User {
  id: ID!
  uri: String!
  username: String!
  name: String!
  friends: [User!]!
  deity: Deity!
}

type Deity {
  id: ID!
  uri: String!
  name: String!
}

type Mutation {
  createAccount(
    firstName: String!
    surname: String!
    password: String!
    deityID: String!
  ): User!
}

type Query {
  user(id: ID!): User!
  deities: [Deity!]!
}
