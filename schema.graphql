type User {
  id: ID!
  username: String!
  name: String!
  surname: String!
  thumb: String
  email: String!
  friends: [User!]!
}

input CreateUserInput {
  username: String
}

type Mutation {
  createUser(input: CreateUserInput!): User
}

type Query {
  user(username: String): User!
  hello: String!
}
