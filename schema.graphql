type User {
  id: ID!
  uri: String!
  username: String!
  name: String!
  friends: [User!]!
}

type Deity {
  id: ID!
  uri: String!
  name: String!
}

type Mutation {
  createUser(username: String!): User!
}

type Query {
  user(id: ID!): User!
  deities: [Deity!]!
}
